               LMDBG - lightweight malloc debugger

It logs the calls of malloc/realloc/free/memalign and nothing else

====================================================================

lmdbg-run --help
lmdbg-leaks --help
lmdbg-sym --help

Sample of usage:

user@host$ lmdbg-run --log /tmp/PROG.memtest PROG ARG1 ARG2 ... ARGN
user@host$ lmdbg-leak < /tmp/PROG.memtest > /tmp/PROG_LEAKS.memtest
user@host$ lmdbg-sym --with-gdb \
   PROG \
   < /tmp/PROG_LEAKS.memtest > /tmp/PROG_LEAKS_SRC.memtest
